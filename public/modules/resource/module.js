/**
 * Abstract module to create services to handle interaction (http calls) related to a collection
 */
(function (ng, undefined) {
    'use strict';
    ng.module('blog-it.resource', []);
})(angular);
